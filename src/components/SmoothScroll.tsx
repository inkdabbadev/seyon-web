// Lenis removed — native browser scroll is used instead.
// This file is kept as a passthrough in case it is imported elsewhere.
export default function SmoothScroll({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
